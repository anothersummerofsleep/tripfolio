{
  "name": "tripfolio",
  "version": "0.1.0",
  "description": "Local-first travel companion — trips, loyalty programs, FX-aware expense splitting, and insurance coverage in plain JSON your AI agents can read.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "mirror": "node scripts/mirror.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.0"
  }
}
